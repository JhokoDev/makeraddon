export interface ManifestTemplateData {
  name: string
  description: string
  author: string
  headerUuid: string
  moduleUuid: string
}

export interface ManifestConfig {
  name: string
  description: string
  author: string
}